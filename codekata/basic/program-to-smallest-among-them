#to print smallest among them
#getting input
N,K=map(int,input("").split())
L=list(map(int,input("").split())) 
length=(len(L))
result=length-1
#print(result)
#function to match the number in k
def smallest():
    c=0
    for i in range(0,result):
        if(K==L[i]):
            c=1
            print ("yes")
    return c             
        
res=smallest()

if(res==0):
    print ("yes")
else:
    print("no")

