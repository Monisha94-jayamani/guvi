const myindex=(array,callback)=>{

    for(let i=0;i<array.length;i++)
    {
       if(array[i]==m)
       {
           count==i;
       }
    }
return -1;
}    

const inp=[1,2,3];
const m=4;
console.log(myindex(inp,m));
